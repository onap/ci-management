---
- project:
    name: doc
    project-name: 'doc'

    jobs:
        - '{project-name}-{stream}-{subproject}-verify-rtd':
            build-node: centos7-basebuild-4c-4g
        - '{project-name}-{stream}-{subproject}-merge-rtd'

    project: 'doc'

    rtdproject: 'onap'

    stream:
        - master:
             branch: 'master'
