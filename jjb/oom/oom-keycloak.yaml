---
- project:
    name: oom-keycloak-project-view
    project-name: oom-keycloak
    views:
      - project-view

- project:
    name: oom-keycloak-info
    project: oom/platform/keycloak
    project-name: oom-platform-keycloak
    build-node: centos7-builder-2c-1g
    jobs:
      - gerrit-info-yaml-verify

- project:
      name: oom-consul-linters
      project: oom/platform/keycloak
      project-name: oom-platform-keycloak
      python-version: python3
      jobs:
          - oom-linters
      subproject:
          - 'yaml':
                tox-dir: '.'
                tox-envs: 'yaml'
                pattern: '**/*.yaml'
          - 'yml':
                tox-dir: '.'
                tox-envs: 'yaml'
                pattern: '**/*.yml'
          - 'py':
                tox-dir: '.'
                tox-envs: 'python'
                pattern: '**/*.python'
      stream:
          - 'master':
                branch: 'master'
          - 'frankfurt':
                branch: 'frankfurt'


- project:
   name: oom-keycloak-release
   project-name: oom-platform-keycloak
   project: oom/platform/keycloak
   mvn-settings: 'oom-settings'
   jobs:
     - '{project-name}-gerrit-release-jobs':
         build-node: centos7-docker-8c-8g

- project:
    name: 'oom-keycloak-docker'
    jobs:
      - gerrit-docker-verify
      - gerrit-docker-merge
    project-name: 'oom-platform-keycloak'
    project: 'oom/platform/keycloak'
    stream:
      - 'master':
            branch: 'master'
            files: ''
    docker-name: 'onap/oom/keycloak'
    docker-build-args: '-f ./Dockerfile'
    docker-root: '$WORKSPACE'
    mvn-settings: 'oom-settings'
    build-node: ubuntu1804-docker-8c-8g
