# Triggers Python test via tox
---
- project:
    name: has-project
    project-name: has 
    project: has 
    mvn-settings: has-settings
    python-version: python3
    build-timeout: 30
    jobs:
      - '{project-name}-python-jobs'
    stream:
      - master:
          branch: master

