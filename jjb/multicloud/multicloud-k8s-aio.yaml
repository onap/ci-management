--
- project:
    name: multicloud-k8s-aio
    project: 'multicloud/k8s'
    build-node: ubuntu1604-builder-4c-4g
    aio_build_script: !include-raw-escape: run-aio.sh

    stream:
      - 'master':
          branch: 'master'

    jobs:
      - '{name}-{stream}':
          build_script: '{aio_build_script}'
