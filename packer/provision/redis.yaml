---
- import_playbook: ../common-packer/provision/baseline.yaml

- hosts: all
  become_user: root
  become_method: sudo

  tasks:
    - name: 'Install Redis'
      package:
        name: redis
        state: present

    - name: 'Start service redis, if not running'
      service:
        name: redis
        state: started
